import { Routes } from '@angular/router';
import { SimulationComponent } from './component/simulation/simulation.component'

export const routes: Routes = [
    { path: 'first-component', component: SimulationComponent },
];
